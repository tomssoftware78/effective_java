Reduce is the repeated process of combining all elements

It takes an input of elements and combines them into a single result by repeated application of a combining
   operation for example, sum, multiplication and division of elements

Methods
   T reduce(identity, BinaryOperator);
   Optional reduce(BinaryOperator);

Optional is a container object which may or may not contain a non-value

